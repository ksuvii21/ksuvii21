name: Generate Snake Animation

on:
  schedule:
    - cron: "0 0 * * *"
  workflow_dispatch:

jobs:
  generate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Generate Snake Animation
        uses: Platane/snk@v3
        with:
          github_user_name: ksuvii21
          outputs: |
            github-contribution-grid-snake.svg
            github-contribution-grid-snake.gif

      - name: Push Snake Animation to GitHub
        uses: EndBug/add-and-commit@v9
        with:
          author_name: github-actions
          author_email: github-actions@github.com
          message: "Generate snake animation"
          add: "github-contribution-grid-snake.svg github-contribution-grid-snake.gif"
        env:
          GITHUB_TOKEN: "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"
